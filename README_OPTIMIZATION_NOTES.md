# Offline transcription optimization

Version 1.2.0 compiles whisper.cpp and ggml with release-grade native optimization even in the directly installable debug APK. It also adds cached model contexts, optional two-worker Turbo processing, 28-second overlapped chunks, near-silence skipping, disk-backed PCM decoding, progress reporting, elapsed-time display, and cancellation.
